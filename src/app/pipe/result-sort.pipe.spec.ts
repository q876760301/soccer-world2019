import { ResultSortPipe } from './result-sort.pipe';

describe('ResultSortPipe', () => {
  it('create an instance', () => {
    const pipe = new ResultSortPipe();
    expect(pipe).toBeTruthy();
  });
});
